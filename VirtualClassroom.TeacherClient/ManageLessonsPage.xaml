<Page x:Class="VirtualClassroom.TeacherClient.ManageLessonsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
	Title="Manage Lessons">

    <Grid>
        <GroupBox Header="Lessons"
                  Padding="5 10 10 10" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <DataGrid x:Name="dataGridLessons" AutoGenerateColumns="False" Margin="0 0 10 0"
                          VerticalScrollBarVisibility="Auto" MaxHeight="600">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" />
                        <DataGridTextColumn Header="Subject" Binding="{Binding Path=Subject}" />
                        <DataGridTextColumn Header="Date" Binding="{Binding Path=Date}" />
                        <DataGridTextColumn Header="Homework Deadline" Binding="{Binding Path=HomeworkDeadline}" />
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel>
                    <Button x:Name="btnAddLesson" Click="btnAddLesson_Click" Padding="5">Add Lesson</Button>
                    <Button x:Name="btnRemoveLesson" Click="btnRemoveLesson_Click" Padding="5">Remove Lessons</Button>
                </StackPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</Page>
