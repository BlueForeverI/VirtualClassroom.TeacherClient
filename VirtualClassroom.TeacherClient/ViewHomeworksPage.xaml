<Page x:Class="VirtualClassroom.TeacherClient.ViewHomeworksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
	Title="View Homeworks">

    <Grid>
        <GroupBox Header="Homeworks">
            <WrapPanel>
                <DataGrid x:Name="dataGridHomeworks" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Subject" Binding="{Binding Path=Subject}" />
                        <DataGridTextColumn Header="Lesson" Binding="{Binding Path=Lesson}" />
                        <DataGridTextColumn Header="Student" Binding="{Binding Path=Student}" />
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel>
                    <Button x:Name="btnDownloadHomework" Click="btnDownloadHomework_Click">Download Homework</Button>
                    <Button x:Name="btnAddMatk" Click="btnAddMatk_Click">Add Mark</Button>
                </StackPanel>
            </WrapPanel>
        </GroupBox>
    </Grid>
</Page>
