<Page x:Class="VirtualClassroom.TeacherClient.ViewHomeworksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
	Title="View Homeworks">

    <Grid>
        <GroupBox Header="Homeworks">
            <WrapPanel>
                <DataGrid x:Name="dataGridHomeworks" AutoGenerateColumns="False" Margin="0 0 10 0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Subject" Binding="{Binding Path=Subject}" />
                        <DataGridTextColumn Header="Lesson" Binding="{Binding Path=Lesson}" />
                        <DataGridTextColumn Header="Student" Binding="{Binding Path=StudentFullName}" />
                        <DataGridTextColumn Header="Has Mark" Binding="{Binding Path=HasMark}" />
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel>
                    <Button x:Name="btnDownloadHomework" Click="btnDownloadHomework_Click" Padding="5">Download Homework</Button>
                    <Button x:Name="btnAddMatk" Click="btnAddMatk_Click" Padding="5" >Add Mark</Button>
                </StackPanel>
            </WrapPanel>
        </GroupBox>
    </Grid>
</Page>
